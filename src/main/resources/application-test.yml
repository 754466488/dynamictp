spring:
  application:
    name: dynamic-tp-nacos-demo
nacos:
  config:
    server-addr: nacos-svc.base.svc.cluster.local:8848
    type: yaml
    data-ids: dynamic-tp-nacos-demo-test.yml
    auto-refresh: true
    group: ${spring.application.name}
    namespace: ${spring.profiles.active}
    bootstrap:
      enable: true
      log-enable: true


